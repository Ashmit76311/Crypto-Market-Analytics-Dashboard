FROM apache/superset:latest

USER root

# Install pymysql directly into the venv's site-packages using the system pip
# targeting the venv's site-packages directory
RUN pip install --no-cache-dir --target /app/.venv/lib/python3.10/site-packages pymysql cryptography

USER superset
